提取ctrllock新函数
报告ctrl发送结果
	报告ctrl发送失败
	报告ctrl发送成功
定义状态机静态变量

查明调用流程和锁的安全问题

以上全部已做

时刻注意避免有符号无符号混合运算

写处理ack的代码

写接收数据的代码

为每个连接状态增加一个函数ump_handle_data_packet用于处理接收到的数据、ack、wnd等信息。

序号、窗口大小等字节序的问题

不能发送太大的单个消息

确认每个变量都经过了恰当的初始化

写处理用户接收数据的代码

发送心跳包

至多每隔一个接收报文就发一个ACK，让发送端能够获得足够的样本来计算RTT

增加重置连接的能力（reset的发送和处理），以应对突发错误